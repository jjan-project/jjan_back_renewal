spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jjan?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: password
  servlet:
    jpa:
      hibernate:
        ddl-auto: create-drop

  sql:
    init:
      mode: never
    show-sql: true